{
  "name": "governance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lucidao-developer/governance",
    "create-local": "graph create --node http://localhost:8020/ lucidao-developer/governance",
    "remove-local": "graph remove --node http://localhost:8020/ lucidao-developer/governance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lucidao-developer/governance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
